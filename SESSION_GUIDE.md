# 🔐 Zara Sniper Bot: Інструкція по роботі з сесіями (zara_auth.json)

Ця інструкція описує, як працює система збереження сесій, де знаходяться файли авторизації та як ними керувати.

## 📌 Як це працює?

1.  **Збереження (Persistence)**: Основна сесія зберігається в **Базі Даних (MongoDB)** у колекції `systemcaches` (ID: `global_zara_session`). Це дозволяє сесії переживати перезавантаження бота та контейнера.
2.  **Файл `zara_auth.json`**: Під час запуску бота, сесія з бази даних вивантажується у тимчасовий файл `zara_auth.json` у системну папку (`%TEMP%`). Браузер використовує цей файл для автоматичного входу.
3.  **Оновлення**: Бот автоматично оновлює сесію в базі даних кожні 15 хвилин та при завершенні роботи.

---

## 🛠 Як створити нову сесію (Перший вхід)?

Якщо бот не може увійти в акаунт або сесія "злетіла", потрібно створити її вручну. Для цього є спеціальний режим `--login`.

1.  **Зупиніть бота** (якщо запущений):
    ```powershell
    Ctrl + C
    ```

2.  **Запустіть режим входу**:
    ```powershell
    npm start -- --login
    ```

3.  **Дії в браузері**:
    *   Відкриється вікно браузера (Chrome/Chromium).
    *   Перейдіть на сайт Zara.
    *   Увійдіть у свій акаунт.
    *   **Важливо**: Дочекайтеся повного завантаження сторінки після входу.
    *   Просто закрийте вікно браузера (хрестик). Бот автоматично збереже нову сесію в базу даних.

4.  **Запустіть бота у звичайному режимі**:
    ```powershell
    npm start
    ```

---

## 📂 Де знайти файл `zara_auth.json`?

Файл створюється динамічно, але якщо він вам потрібен для дебагу або ручного бекапу, його можна знайти тут:

*   **Windows**: `%TEMP%\zara_auth.json`
    *(Зазвичай це: `C:\Users\Ім'я\AppData\Local\Temp\zara_auth.json`)*
*   **Linux / Cloud**: `/tmp/zara_auth.json`

> **Увага**: Цей файл перезаписується при кожному старті бота даними з MongoDB. Змінювати його вручну під час роботи бота немає сенсу — зміни будуть втрачені при наступному перезапуску. Якщо треба змінити сесію навсправжки — використовуйте режим `--login`.

---

## 💾 Резервне копіювання (Advanced)

Якщо ви хочете зробити "вічний" бекап сесії (наприклад, перед переїздом на інший сервер):

1.  Відкрийте вашу базу даних MongoDB (через MongoDB Compass).
2.  Знайдіть колекцію `systemcaches`.
3.  Знайдіть документ з `_id: "global_zara_session"`.
4.  Скопіюйте вміст поля `data` (це і є JSON вашої сесії).
5.  Збережіть його у текстовий файл.

Щоб відновити:
1.  Вставте цей JSON назад у поле `data` в MongoDB.
2.  Перезапустіть бота.
