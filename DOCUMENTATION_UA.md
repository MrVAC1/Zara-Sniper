# 📘 Технічна Документація Zara Sniper Bot (v4.6)

## Вступ
Це спеціалізоване програмне забезпечення (бот) для автоматичного відстеження та швидкого викупу товарів на сайті Zara. 

**Версія 4.6** використовує вдосконалений **гібридний підхід**:
1.  **Hunter (API)**: Моніторинг наявності через швидкі API-запити.
2.  **Killer (Browser)**: Блискавичне оформлення замовлення через Playwright.
3.  **Queue Manager**: Інтелектуальна черга з пріоритетами для критичних залишків (`low_stock`).

---

## 🏗️ Архітектура та Основні Компоненти

### 1. `sniperEngine.js` (Основне Ядро)
Центральний модуль, що керує життєвим циклом завдання:
*   **Фаза 1: API Monitoring**: Циклічна перевірка доступності SKU через endpoint catalog.
*   **Фаза 2: Auto-Correction**: Якщо API каже "OK", але SKU не знайдено, бот автоматично завантажує сторінку (`viewPayload`), щоб знайти актуальний ID розміру.
*   **Фаза 3: Execution**: Запуск браузера, вибір розміру та додавання в кошик.
*   **Фаза 4: Checkout Queue**: Глобальна черга, що запобігає конфліктам при одночасному викупі.

### 2. `zaraApi.js` (Розвідка)
Відповідає за спілкування з серверами Zara:
*   Виконує "легкі" GET-запити для перевірки стоку.
*   Обробляє помилки 403/401 (Akamai) та ініціює паузу.

### 3. `healthGuard.js` (Самозахист)
*   Періодично (кожні 14 хв) перевіряє "здоров'я" сесії.
*   Якщо виявлено блокування IP, ставить систему на паузу (IP Guard, 60 хв).

---

## 🚀 Нові Функції (v4.5 - v4.6)

### 🥇 Priority Queue (Пріоритетна Черга)
Бот тепер розрізняє статус наявності:
*   **`low_stock` (Мало)**: Отримує **Високий Пріоритет (1)**. Обганяє інші завдання в черзі на викуп.
*   **`in_stock` (Є)**: Отримує **Звичайний Пріоритет (0)**.

### 🔒 Global Checkout Lock (Семафор)
Щоб уникнути "каші" в браузері, одночасно купувати може лише одне завдання.
*   Інші чекають у "Кімнаті Очікування" (Waiting Room).
*   Захисний механізм (Watchdog) автоматично знімає блокування через 120 с, якщо процес завис.

### 🛡️ API Re-Verification (Подвійна Перевірка)
Перед самим переходом до оплати (після отримання доступу до черги), бот робить контрольний API-запит.
*   Якщо товар зник за час очікування — викуп скасовується, щоб не гаяти час.
*   Якщо товар є — переходимо до оплати.

### 🔧 SKU Auto-Correction
Якщо при старті завдання вказано застарілий `skuId` (який не відповідає API), бот:
1.  Не зупиняється з помилкою.
2.  Відкриває сторінку товару.
3.  Зчитує `window.zara.viewPayload`.
4.  Знаходить новий правильний ID за Кольором та Розміром.
5.  Оновлює базу даних і продовжує полювання.

### 🧹 Safe Cleanup
*   **Smart Tab Closure**: Вкладка браузера не закривається, поки SKU не буде успішно провалідовано хоча б раз. Це дозволяє власнику побачити проблему, якщо вона є.
*   **Auto-Delete Screenshots**: Після успішного проходження 3D Secure (успішний викуп) скріншоти автоматично видаляються з диску для економії місця.

---

## ⚙️ Конфігурація (`.env`)

Основні параметри для налаштування поведінки:

| Змінна | Опис | Рекомендовано |
| :--- | :--- | :--- |
| `MIN_DELAY` / `MAX_DELAY` | Діапазон затримок для імітації людини (hover, рух миші) | `200` / `600` |
| `DELAY_CHECKOUT_STEP` | Пауза після натискання кнопки "Continue" | `2000` |
| `GOTO_TIMEOUT` | Макс. час завантаження сторінки | `60000` |
| `AKAMAI_BAN_DELAY` | Час "відсиджування" при блокуванні IP (мс) | `3600000` (1 година) |
| `SNIPER_INTERVAL` | Пауза між API запитами (базова) | `1500` |

---

## 🐞 Вирішення Типових Проблем

### "IP BLOCKED TEMPORARILY"
Система `healthGuard` виявила, що Zara не віддає контент.
*   **Дія**: Бот стає на паузу (60 хв). Не чіпайте його, це захист.

### "SKU NOT FOUND IN RESPONSE"
API повернуло `200 OK`, але шуканого ID там немає.
*   **Дія**: Бот автоматично запустить **Auto-Correction**. Просто зачекайте 1-2 хвилини.

### "ReferenceError: GOTO_TIMEOUT is not defined"
(Виправлено у v4.6) Помилка виникала у модулі Health Check. Зараз виправлена.


### ⚙️ Технічні налаштування (Config)
DEBUG_API=true — Вмикає детальний лог відповідей від Zara (показує JSON відповіді). Корисно при помилках.
LOG_LEVEL=info — Рівень деталізації консолі.
ENABLE_SCREENSHOTS=true — Чи робити скріншоти кроків.
HEADLESS=false — false означає, що браузер видимий. true — прихований (швидше, але частіше ловлять ботів).


### 🏎️ Швидкість та Моніторинг
API_MONITORING_INTERVAL=500 — Основна частота запитів до API (0.5 сек).
SNIPER_INTERVAL=10000 — Фолбек-інтервал (якщо API недоступне).
AKAMAI_BAN_DELAY=45000 — Скільки чекати, якщо зловили бан 403 (45 сек).


### 🖱️ Імітація Людини (Stealth)
MIN_DELAY=0 / MAX_DELAY=200 — Випадкова пауза перед наведенням/кліком (мілісекунди). Чим менше, тим швидше, але небезпечніше.
CLICK_DELAY=200 — Час самого натискання кнопки миші.


### ⚡ Оформлення Замовлення (Checkout)
GOTO_TIMEOUT=10000 — Макс. час на завантаження сторінки (10 сек).
TIMEOUT_PAY_BUTTON=3000 — Скільки чекати кнопку "Оплатити".
DELAY_CHECKOUT_STEP=200 — Пауза між натисканням "Продовжити" (дуже важливо для стабільності).
DELAY_POST_CVV=2000 — Очікування після введення CVV, перед фінальним кліком.
DELAY_3DS_SUCCESS=3000 — Скільки "милуватися" сторінкою 3D Secure перед відправкою скріншоту.


### 🕒 Додаткові таймінги (рідше впливають)
SELECTOR_TIMEOUT=10000 — Скільки чекати появи будь-якого елемента (кнопки, інпуту) перед тим як викинути помилку (10 сек).
TIMEOUT_SIZE_MENU=2000 — Скільки чекати відкриття меню розмірів.
TIMEOUT_3DS_REDIRECT=3000 — Час очікування редіректу на 3D Secure.
TIMEOUT_API_RETRY=500 — Якщо API повернуло помилку мережі, бот чекає 0.5 сек перед повтором.
ACTION_PAUSE=1000 — Загальна пауза між "великими" діями (наприклад, перехід на нову сторінку).
DELAY_CAPTCHA_SOLVE=30000 — Час, який дається вам на ручне проходження капчі (30 сек), якщо вона вилізла.


### 🛡️ Захист та Відновлення
IN_STOCK_RECOVERY_TIMEOUT=5000 (5 сек) — Якщо товар є, а бот завис на 5 сек — він перезавантажить сторінку.
DELAY_RECOVERY_WATCHDOG=8000 (8 сек) — Глобальний сторожовий пес для завислих процесів.
TIMEOUT_MODAL_CHECK=500 — Швидкість перевірки модалок (Sold Out popup).


### 🧩 Системні (краще не чіпати)
TIMEOUT_DB_RETRY / TIMEOUT_LOOP_RETRY (3 сек) — Паузи при внутрішніх помилках (база даних, збій циклу).
TIMEOUT_FAST_SELECTOR (1 сек) — "Швидка" перевірка (наприклад, "чи з'явилося вікно помилки?"). Якщо ні — йдемо далі без затримок.
DELAY_WATCH_LOOP (0.3 сек) — Частота "пульсу" в циклах очікування (щоб не грузити процесор).


---

**Розроблено на замовлення.**
*Останнє оновлення: 15.01.2026*
